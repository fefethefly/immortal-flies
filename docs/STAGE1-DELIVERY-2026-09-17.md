# 第一阶段收尾状态（2026-09-17）

## 本轮已实现并在本地验证

- 孵化/繁殖共用单个非重叠区块轮询；刷新读取链上未完成繁殖请求，提供过期清理。
- 钱包账号、链与断开事件重置状态，废弃旧会话异步结果。
- 转移后的栖息地 owner 同步；保留本地能量和位置，解除旧拖动。
- 按 chainId + collection 分隔本地养育存档，按 lifeId 恢复身体状态；不从存档恢复所有权。
- 每 3 秒、页面隐藏/离开时保存。隐藏页面停止模拟，无离线补算。最近链上刺激的本地去重游标持久化。
- 中英文明确提示本地模拟、损坏/不可用存储与恢复状态。不是正式链上生命档案。
- 构建不再删除完整连接组；构建前核验主网清单钉住的 manifest、三个图数据文件和六个运行器文件。未改 genesisRoot、合约或历史包。

## 验证

- `npm test`：190/190。
- `npm run build`：通过。
- `node scripts/verify-life-genesis.mjs dist`：9 个文件大小/指纹通过。
- `node scripts/test-habitat-browser.mjs`：自行构建、启动本地服务和无头 Chrome；模拟 RPC，不访问公共链或发送交易。真实页面投食能量 429 → 649，刷新仍为 649，显示恢复提示，无 runtime exception。测试启用减少动态效果以冻结模拟。

## 尚未验收 / 发布边界

- 未部署官网、未 commit、未发送公共链交易。线上资源恢复必须发布后再次验证。
- 真实钱包孵化/繁殖两次签名、拒签、转移和移动端仍需端到端验收；现有请求测试验证纯逻辑与事件模拟，不等于真实钱包验收。
- 正式 Journal 完整输入历史 → 神经运行器 → 连续检查点 → 独立恢复 UI 尚未闭环。当前栖息地仅对最新刺激做本地反馈，不宣称全历史重放。
- 本地养育可编辑、同浏览器共享，不随 NFT 转移到另一台设备；清除网站数据会丢失。食物粒子/阵风暂态不在本地存档中。
- `public/data/malecns-full` 仍被 Git 忽略。移除了 Vercel 上传排除，但 Git 集成部署必须在构建环境补齐原始、匹配指纹的文件；缺失会阻止构建。不要重新生成出生承诺来绕过检查。
- 旧创世 hash 包保留，不删除历史引用。未增加代币机制，未改变亲代性状生成或主网身份核。
