# IMMORTAL — 设计与交互说明

## 当前首页：数字生命观测台（2026-09）

首页以用户确认的金融网络视频为视觉参考，采用宽幅观测台：上方品牌文案与入口，中间点云果蝇，左右六个仪表面板。旧金与骨白保留品牌连续性；神经活动使用冷灰绿，复眼使用红色。这一首页方向取代下文旧版首页的视觉限制，其他页面沿用各自设计。

- 果蝇为确定性生成的三维点云：一对透明翅膀、翅脉、红色复眼、分节腹部、六足、触角与平衡棒。粒子是美术几何，不是生物神经元数量。
- 中央 24 个神经读数、外围个体、交易与谱系来自现有本地纸面模型；不将它标成完整 MaleCNS 实验。
- 神经、社会、市场三种聚焦模式每 9 个模拟 tick 自动巡览；手动选择后固定，自动巡览按钮恢复。选中节点同步更新神经仪表与个体编号。
- 暂停按钮冻结模拟、轮换观察对象与画布动画。后台页暂停模拟；画布出视口停止绘制，设备像素比上限 2，动画约 30fps；低动态模式取消连续动画并保留状态读数。
- 真实 IFS 合约仍由原有官方合约栏展示。Credit、金库与结算标为待接入；纸面价格和账本明确标注 SIM，不作为 IFS 实时行情或真实收益。
- 模块边界：`home-observatory.jsx` 负责展示与观察交互，`observatory-art.mjs` 负责点云几何，`observatory-renderer.mjs` 负责动画与资源清理，`home-observatory.css` 负责局部样式；保持生命和金融内核不变。
- 强动效：整片翅膀以可辨识的演示节奏绕翅根振动（约 5.7Hz，为艺术化慢放，并非真实果蝇翼频），翅脉跟随膜面；三层稀疏残影、旋转扫描光环、轨道粒子拖尾与身体流光共同表达活动。红色复眼始终保留。
- 神经、行为、谱系、价格与账本卡片只在对应数据变化时触发 600–680ms 扫光/亮边。新成交与脉冲变化触发扩散环。未接入的经济卡片不制造虚构活动。所有装饰动画遵循暂停、离屏与低动态模式。
- 移动端把果蝇置于上方，六个仪表改成双列；所有个体节点均可用键盘选择。支持中英文。

## 旧版其他页面的视觉核心

「维特鲁威果蝇」：人执圆规，把一只果蝇量进圆与方，再写回生命。品牌面是达芬奇手稿遇见召唤印记，不是赛博标本馆，也不是哥特教堂。

- 底 `#070706`，表层 `#100f0c`，正文骨白 `#e8e2d6`。
- 圣痕只用旧金 `#b08a4a`：活着、转生、署名。
- 神经读数可用灰绿 `#7d8b6e`；品牌面不再使用酸绿、青霓虹或紫电。
- 标题 Cormorant Garamond + 宋体；批注与哈希 IBM Plex Mono。
- Logo 是准星圆 + 外接方 + 白线果蝇。Favicon、页眉、卡面盖同一枚印。

## 页面节奏

1. 交易坑 `/swarm.html`：产品主循环。左坑右案：先看见转向，再看见下单。名册是印章，成交是河，不是总表。
2. 祭台（本地 `altar.html`，生产环境并入蓝图页）：维特鲁威印记与身份仪式；钱包入口仍是「署名」。
3. 谱系：三枚签印，不是全息游戏卡。
4. 生命延续原则：身份、状态、复原。交易是它做的事，不是它是谁。
5. 书记库：导出/恢复与真实事件记录。

## 交易坑内的交易世界六视图（2026-09，新增）

交易坑 `/swarm.html` 顶部是七个视图的页内切换（箭头键可操作）：交易坑 / Colony / Intent / Risk / Execution / Vault / IFS。交易坑保留左坑右案主循环；六个产品视图读同一份纸面世界（`src/brain/flyswarm/world.mjs`，L2 解释层），不写内核。

- Colony：社会状态条（CONSENSUS / SPLIT / NO_QUORUM / STALE）+ 蜂巢压力计（0=卖压、100=买压）+ 活场（最近 16 tick 六类事件按 tick 落列：感觉旧金、行为灰绿、记忆米金、成交按买卖着色、社会与风控锈红）+ 因果条（起因 → 箭头 → 结果，点击重放整条链）+ 影响列表 + ask 通道（七个固定问题本地确定性回答；API+LLM 可用时开放自由提问）。
- Intent：端口管线（行为分布 → TradePort → RiskPolicy → CreditPolicy → 授权 → Executor → 回执）+ 每蝇双栏卡片：原生行为学（动作、左右运动、感觉计数）与端口读取（BUY/SELL/HOLD、置信），转向标「转向」。原生语言与解释永远分栏，不混写。
- Risk：蜂巢权益/流动性/集中度/回撤/信用五个 KPI + 仓位表（份额条对上限）+ 拒绝记录 + 版本化策略参数。全部是纸面审计注解，不授权资金。
- Execution：成交回执表：tick、蝇、方向、名义额、滑点、税、裁定（PASS / FLAG 带原因）。回执是解释层派生的审计记录，不撤回已发生的纸面成交。
- Vault：用户金库标「未接入」（份额/退出队列为空，见 PRODUCT-LATEST §11）；蜂巢金库披露净值、高水位、已实现盈余、回购预算、费用与准备金，净值/高水位逐 tick 柱图。浮盈不形成回购预算。
- IFS：官方代币事实（主网地址链到 bscscan）+ 六个纸面面板（买入未开放、锁仓门槛、抵押只可支撑一次额度、费用、价值预算、回购只由已实现盈余触发且路由未接通）。不把锁仓称为销毁。

LLM 边界抽屉（每个视图右上「解释（本地）」打开，ESC 关闭）：解释/检索/候选计划/工具白名单/策略验证五个标签。当前是本地确定性解释器（`explain.mjs`）；`npm run server` + LLM 密钥时叙述层换成远端模型，证据步骤与校验规则不变，模型缺席仍可运行。全部数据标注 SIM；「改变一次经历」创建个人分支，不改变主线。

## 卡面系统

固定层：圆、方、Soul ID、出生批注。

当前原型：Amber 对应可训练的 #0001；Echo 与 Phantom 展示同一印记的不同金属倾向。三者均明确标为未发行。

正式版本扩展：觉醒/休眠/转生只改变线的明暗与旧金，不改变身份。不把转生次数直接转换成稀有度。

## 动效与交互

- 印记胸腔光核缓慢呼吸；休眠后停止并降低亮度。
- 训练出现圆环，实际习性值更新。
- 16 个神经节点依照当前电位与脉冲发光；连线布局为示意。
- 卡片跟随指针轻微倾斜。
- 模拟页不可见时暂停；画布离开视口停止绘制。
- 支持 `prefers-reduced-motion`；声音默认关闭。
- 互斥操作防止训练/恢复/挑战互相覆盖；损坏档案不能造成白屏。

## 工程与产品边界

首页不显示虚构市值、成交量或持有人数。蜂群页可以显示纸面权益，必须标明 `SIM`。1024 是身份上限。未部署时钱包只是本地署名；测试网部署后，署名会切到 chainId 97 并读写 ImmortalFly。迷宫与本地档案仍不是链上成绩。印记美术与链上基础 SVG 区分清楚。

神秘感应来自「量过、写过、还能醒来」。连接组是 Janelia MaleCNS 成年雄蝇 CNS，不是幼脑；复活是项目规则。$IFS 代币已在 BSC 主网发射，NFT 合约主网未开放；测试网闭环见 docs/TESTNET.md。不把 MaleCNS 写成神的大脑。数据流见 docs/BIOLOGY-SPINE-V6.md。
